/** Shared anchor id for guide-download forms (scroll targets from hero CTAs). */
export const M2M_GUIDE_FORM_SECTION_ID = "guide-form" as const

/** Default hero / secondary CTA label when a page does not pass custom children. */
export const DOWNLOAD_GUIDE_BUTTON_LABEL = "Download the Guide" as const

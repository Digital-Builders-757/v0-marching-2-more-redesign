# Marketing images (`public/images`)

Structured campaign art lives beside route intent:

| Folder | Purpose |
|--------|---------|
| `site/` | Homepage hero backgrounds, homepage sell-band, circular partner vignette |
| `facing-foreclosure/` | `/facing-foreclosure` hero, guide carousel slides, lead section |
| `divorce/` | `/navigating-divorce` hero + collage panels + valuation/aerial bleed |
| `downsizing/` | `/downsizing-your-home` hero + supporting sections |

**Inbound batch:** rename on drop with `m2m-{slug}.png`; keep a verbatim copy tree in `_original-client-delivery/`.

**Full map:** [`docs/M2M_ASSET_MAP.md`](../../docs/M2M_ASSET_MAP.md).

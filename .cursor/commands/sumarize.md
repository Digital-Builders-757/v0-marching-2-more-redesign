/sumarize

Use **`/summarize`** instead — open **`summarize.md`** (same intent: handoff prompt for the next agent).

This file remains only if your Cursor workspace still maps the typo `sumarize` to this path.

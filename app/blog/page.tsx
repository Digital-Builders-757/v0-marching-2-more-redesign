import { redirect } from "next/navigation"

/**
 * Blog is temporarily disabled — set aside until content is ready.
 * @see docs/WORK_ORDER.md
 */
export default function BlogIndexRedirectPage() {
  redirect("/")
}

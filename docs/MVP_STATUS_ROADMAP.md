# MVP status / roadmap — Marching 2 More (marketing site)

High-level phase view. Detailed “done / next” lives in **[WORK_ORDER.md](./WORK_ORDER.md)**.

**GHL phase:** The project is in **implementation hardening / live cutover** mode — the **website-side lead capture foundation is shipped** (`POST /api/submit-lead`, wired forms, booking helper, observability). **Live QA** has largely validated the stack (incl. DOB + TEXT **Urgency** in GHO, with **operator contact-layout visibility** documented as distinct from “failed writes”). **Canonical handoff:** [M2M_WEBSITE_TO_GHL_SYSTEM_GUIDE.md](./M2M_WEBSITE_TO_GHL_SYSTEM_GUIDE.md) and [M2M_CLIENT_CRM_HANDOFF_GUIDE.md](./M2M_CLIENT_CRM_HANDOFF_GUIDE.md); matrix/operator/master plan aligned. Ongoing: **`GHL_*`** in Vercel, admin checklist, runbook, workflows/calendars, **`GOHIGHLEVEL_*`** in **`lib/m2m-site.ts`**. See **[M2M_GHL_REMAINING_GAPS.md](./M2M_GHL_REMAINING_GAPS.md)** for done vs blocked vs next operator steps.

## Current focus (P0)

- **GoHighLevel cutover (ongoing toward live sub-account)** — **Done in repo:** lead API, `lib/ghl/`, form wiring, `getPrimaryConsultationBookUrl()`, runbook/gaps docs, Playwright API + funnel regression suites in **`npm run ci`**. **Not done (host / GHL):** populate **`GHL_*`** in Vercel from **[`.env.example`](../.env.example)**; complete **GHL admin** tasks in **[M2M_GHL_ACCOUNT_SETUP_CHECKLIST.md](./M2M_GHL_ACCOUNT_SETUP_CHECKLIST.md)**; execute **[M2M_GHL_LIVE_CUTOVER_RUNBOOK.md](./M2M_GHL_LIVE_CUTOVER_RUNBOOK.md)** sequences against production. Replace **`GOHIGHLEVEL_*`** placeholders in **`lib/m2m-site.ts`** when booking/quiz URLs exist.
- **Wix / content parity QA** — Home, buy, sell, team, contact, policies; CTAs and numbers match **`lib/m2m-site.ts`** (partner URLs: **`PARTNER_LINKS`**; review CTA: **`GOOGLE_REVIEW_URL`**).
- **Site modernization** — Public routes in **[WORK_ORDER.md](./WORK_ORDER.md)** use **`M2mInsetHeroFrame`** / **`M2mInsetHeroScrim`** for the inset hero cluster (home, home-search, resources, CMA, free valuation), refreshed execution table (website-only), and polish on policy shell, agent profiles, blog, contact trust row, and footer **`M2M_MEDIA`**. **Shipped (2026-04):** **site experience uplift** (homepage value clarity, fewer competing hero CTAs, nav grouping, contact reassurance strip, shared safe-area / rhythm)—see **Done recently** in WORK_ORDER and `docs/marching2more/*_WORK_ORDER.md`. **`main#main-content` overflow-x** clamp + shared marketing CTA **`min-h-12`** / focus rings (**2026-04 priority-guide pass**); home funnel (**`PropertySearch`**, **`SellHero`**, testimonials, **`/home-search`**): tap targets / buyer intent / checklist terms link (**home funnel follow-up**). **Shipped (2026-05):** **layout primitives** (`min-w-0` on inset hero / section / prose), **footer** + **policy** mobile rhythm, **`m2mFormShellWidthSafeClass`**, **DOCUMENTATION_INDEX** queue vs ship-log note — see **Repo hygiene + mobile polish** in **Done recently**. Campaign landings may use **semantic keys** in **`lib/m2m-media.ts`** (`divorce*`, `vaLoan*`, `fha*`, `credit*`, `investorCarousel*`) so final Blob swaps stay centralized. Shared **lead-submit error** UI, **form submit tokens** (`lib/m2m-form.ts`), **header/footer** focus + touch targets, **skip link** + selection/reduced-motion CSS, **`M2mSection` density** / **`M2mProse`** helpers. **Shipped (2026-05):** **`typescript.ignoreBuildErrors`** removed — `npm run ci` + **`next build`** enforce TypeScript. Ongoing: replace interim aliases with commissioned photography.
- **Launch polish (2026-05 batches 2–5)** — **Repo:** softer route loading (**`M2mRouteLoading`**, Suspense/contact shell, **`globals`** shimmer), hardened **`/contact-us`** **`lead_type`** syncing from **`intent`** only, **`/free-home-valuation`** + **`/reviews`** card framing aligned to **M2m** borders/rings, proof/smoke pointers under **`docs/proof/`**, and GHL/QA doc refresh. **Follow-on ship:** **`/our-team`** + profiles (**`AgentProfile`**), **`/blog`** hero/list (**`next/image`**, **`M2mContainer`**), reviews hero stability, **`M2mConsultationCta`** focus rings + contact Suspense shell gradient parity, **[`M2M_ASSET_MAP`](./M2M_ASSET_MAP.md)** Blob team/agent table. Canonical log: **[WORK_ORDER.md](./WORK_ORDER.md)** **Done recently**. **Mandatory pre-merge gate remains** **`npm run ci`** (`lint`, **Vitest**, **typecheck**, **`next build`**, Playwright).
- **Lead-gen / routing** — Shared **`M2mBrandLogo`** + **`M2mLeadQuizSection`**; campaign routes post to **`/api/submit-lead`**. **Static quizzes** under **`public/quizzes/`** (downsizing + navigating-divorce) embed via **`isQuizEmbedSrcConfigured`** (`/quizzes/...` or `https://`); quiz HTML/JS submits to the same API with answers in **`notes`**, and **awaits HTTP + JSON `ok`** before showing irreversible success UI (**2026-05**). **`M2mLeadQuizSection`** supports **`embedVariant`** (`standard` / `tall`) and a framed iframe shell; downsizing/divorce campaign sections and **`DownsizingFallbackLead`** have presentational polish; **`FacingForeclosureQuizFallbackLead`** fills the foreclosure quiz block when **`GOHIGHLEVEL_QUIZ_FORECLOSURE_URL`** is unset. Remaining **`GOHIGHLEVEL_QUIZ_*`** placeholders in **`lib/m2m-site.ts`** still apply to **credit** and **investor** embeds (and foreclosure **iframe**, once a real URL replaces the fallback-only state) until those assets ship. **`phone`** / **`date_of_birth`** optional on the API; DOB uses shared **`M2mLeadDobField`** (Radix **Select**, portaled) with local **Y/M/D** state → **`YYYY-MM-DD`** (reliable inside **`M2mInsetHeroFrame`** `overflow-hidden`; **`/home-search`** form **`z-20`**). **Urgency** TEXT is populated on **all** wired forms (short forms default **“Not sure yet”**; `urgency_explicit` logged server-side). **Strict success contract (2026-05):** success now requires full CRM completion (contact upsert + tags + opportunity + note); any required-step failure returns `ok: false` with `correlationId` and step context for operator triage. **CRM errors:** tiered **`crm_validation`**, duplicate hints, **`crm_unreachable`** for transport failures — **live GHO validation** via cutover runbook + [M2M_LEAD_CAPTURE_QA.md](./M2M_LEAD_CAPTURE_QA.md).
- **CI health** — `npm run ci` (lint, Vitest, typecheck, build, Playwright e2e) stays green locally and on PRs (`.github/workflows/pr-ci.yml` matches that sequence).
- **Content engine + launch readiness (2026-04)** — Live `/blog` (index + posts, subset renderer, metadata/JSON-LD where applicable), `lib/m2m-content-clusters.ts` + `M2mRelatedPages` internal bands, homepage `HomeTopicNav`, `M2mCtaAnalytics` / `data-m2m-track` → `m2m_cta`, campaign `M2mCampaignFaq` + metadata layouts (`contact-us`, `cma-form`), reviews hero `Image`, launch a11y/SEO (single document `h1` on `/home-search`, star rating sr-only, focus rings on cluster links, homepage canonical/OG, drop root `generator` meta). **2026-05:** follow-on pass — post-submit next-step nav, cluster/consultation wiring, seller bridge on `/home-search`, blog CTA trio + buy/sell links (see **Conversion + content engine** in Done recently). Framing doc: [marching2more/M2M_CONVERSION_AND_CONTENT_ENGINE_WORK_ORDER.md](./marching2more/M2M_CONVERSION_AND_CONTENT_ENGINE_WORK_ORDER.md); ships: **Done recently** in [WORK_ORDER.md](./WORK_ORDER.md).

## Next (P1)

- **Production media** — Replace placeholder remote images with finalized assets; keep **`next.config.mjs`** `images.remotePatterns` in sync. Campaigns **`/improve-your-credit`**, **`/downsizing-your-home`**, **`/va-loan-benefits`**, and **`/facing-foreclosure`** use `content.ts` “SWAP” / TODO notes for final art and links; lead forms on those routes post to **`/api/submit-lead`** (GHL env required for live CRM).
- **Broader automation** — **`vitest`** covers GHL classifier / validate / mapping. Playwright (**`tests/e2e/`**, part of **`npm run ci`**) includes funnel regression, consultation guardrails, and **`submit-lead-api.spec.ts`** — see **`docs/proof/E2E_SMOKE_PATHS.md`**. Optional: extend coverage or run live GHO browser E2E when prioritized.
- **TypeScript in deploy** — **Done (2026-05):** `next.config.mjs` does not set `ignoreBuildErrors`; keep **`npm run ci`** green before merge.

## References

| Doc | Purpose |
|-----|---------|
| [AGENTS.md](../AGENTS.md) | Agent start + canonical doc order |
| [ARCHITECTURE_CONSTITUTION.md](./ARCHITECTURE_CONSTITUTION.md) | Invariant list |
| [ARCHITECTURE_SOURCE_OF_TRUTH.md](./ARCHITECTURE_SOURCE_OF_TRUTH.md) | Technical overview |
| [WORK_ORDER.md](./WORK_ORDER.md) | Recent ships and ordered next steps |
| [M2M_GHL_REMAINING_GAPS.md](./M2M_GHL_REMAINING_GAPS.md) | GHL: done vs blocked vs QA |
| [M2M_GHL_LIVE_CUTOVER_RUNBOOK.md](./M2M_GHL_LIVE_CUTOVER_RUNBOOK.md) | Env + test order for cutover |
| [DOCUMENTATION.md](./DOCUMENTATION.md) | Short stub linking to [DOCUMENTATION_INDEX.md](./DOCUMENTATION_INDEX.md) |
| [lib/m2m-site.ts](../lib/m2m-site.ts) | Phone, mailto, GHL/Calendly booking, partners, resource URLs |
